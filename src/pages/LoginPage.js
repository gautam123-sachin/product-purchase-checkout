import React from 'react';
import Login from '../components/auth/Login';

const LoginPage = () => {
  return (
    <div>
      {/* You can add a common layout, like Header and Footer, here */}
      <Login />
      {/* You can add a common layout, like Header and Footer, here */}
    </div>
  );
};

export default LoginPage;
